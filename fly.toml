app = 'telegram-keyword-monitor-bot'
primary_region = 'iad'

[build]
  builder = 'paketobuildpacks/builder:base'

[[processes]]
  type = 'worker'
  cmd = 'python main.py'

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024